spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
      username: config-user
      password: config-pass
      fail-fast: true
