apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: secure-distributed
  labels:
    app: order-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: order-service
        version: v1
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      initContainers:
        - name: wait-for-eureka-server
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for eureka-server to be ready..."
              until wget -qO- http://eureka-server:8761/actuator/health 2>/dev/null | grep -q '"status":"UP"'; do
                echo "eureka-server not ready, retrying in 5s..."
                sleep 5
              done
              echo "eureka-server is ready!"
      containers:
        - name: order-service
          image: secure-distributed/order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              protocol: TCP
          env:
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server:8888"
            - name: SPRING_CLOUD_CONFIG_USERNAME
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: config-user
            - name: SPRING_CLOUD_CONFIG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: config-password
            - name: EUREKA_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: eureka-user
            - name: EUREKA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: eureka-password
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://$(EUREKA_USER):$(EUREKA_PASSWORD)@eureka-server:8761/eureka"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: secure-distributed
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: order-service-pdb
  namespace: secure-distributed
  labels:
    app: order-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: order-service
