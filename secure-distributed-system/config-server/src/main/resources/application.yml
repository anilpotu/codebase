server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.home}/secure-distributed-system/config-repo
        git:
          uri: file:///${user.home}/secure-distributed-system/config-repo
          default-label: main
          clone-on-start: true
  security:
    user:
      name: config-user
      password: config-pass

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
